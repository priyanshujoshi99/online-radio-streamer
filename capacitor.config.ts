import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.priyanshujoshi99.onlineradiostreamer',
  appName: 'Online Radio Streamer',
  webDir: 'dist'
};

export default config;
